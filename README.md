Theme Changer is a React application that lets users switch the page background between multiple colors. The color options are passed as props to a child component, and useState manages the active theme. By default, the theme is white, and useEffect dynamically applies the chosen background in real-time.
